# Поэтапная Сборка И Проверка MIC_AI REV 2.0

> **Статус:** этапы ниже относятся к legacy-сборке Blue Pill. Для новой связки `NUCLEO-G431RB + X-NUCLEO-IHM09M2` сначала использовать [NUCLEO_G431_MIGRATION_RU.md](NUCLEO_G431_MIGRATION_RU.md); распиновки и питание двух вариантов не смешивать.

Эта инструкция относится к схеме [MIC_AI_REV2_SCHEMATIC.pdf](output/pdf/MIC_AI_REV2_SCHEMATIC.pdf). Каждый этап выполняется только после успешного предыдущего.

## Этап 0. Ничего Не Питать

1. J7/HV физически отключить.
2. Сетевой вход 230VAC отключить.
3. Разрядить DC-шину и измерить напряжение мультиметром.
4. ST-Link, USB и Saleae подключать только по правилам доменов из `CONNECTION_MATRIX_RU.md`.
5. Не использовать старую прямую связь земель UNO Q/Saleae с Blue Pill.

Критерий перехода: на шине безопасное напряжение, источники отключены.

## Этап 1. Собрать И Прозвонить Земли

Собрать только разъёмы и изоляторы, без сети и J7.

Омметром проверить:

- `SAFE_GND-HOT_GND`: разрыв;
- `SAFE_GND-PE`: разрыв;
- `HOT_GND-PE`: разрыв;
- `AC_N-HOT_GND`: разрыв при снятом BR1;
- все J2 GND-контакты объединены только с `HOT_GND`;
- под корпусами ISO7721/ISO7740 нет меди, перемычек и полигонов между доменами.

Критерий перехода: ни одной непредусмотренной связи SAFE/HOT/PE/AC_N.

## Этап 2. Проверить SAFE Питание

1. Подать UNO Q через VIN `7..24V` или USB-C, но не через 5V/3.3V pins.
2. Выбрать `UNO` или `EXT` на `JP_SAFE_3V3`.
3. Проверить `SAFE_3V3` на безопасной стороне ISO7721/ISO7740.
4. Подать отдельные `SAFE_5V` на контур аппаратного запрета PWM.
5. Не подавать HOT_15V.

Критерий перехода: SAFE-напряжения нормальны, HOT-сторона остаётся обесточенной.

## Этап 3. Подать Только Внешние HOT 15 В

1. J7/HV и 230VAC остаются отключены.
2. Использовать гальванически изолированный лабораторный источник 15 В.
3. Подключить его к `J_HOT15_EXT` и поставить `JP_HOT15_SRC=EXTERNAL`.
4. Ограничить ток источника и включить без Blue Pill/STEVAL, затем с нагрузками по одной.
5. Измерить `HOT_15V`, `HOT_12V`, `HOT_5V`, `HOT_3V3` относительно `HOT_GND`.
6. Проверить J4: `+ = HOT_15V`, `RETURN = HOT_GND`.

Критерий перехода: напряжения стабильны, ток покоя ожидаем, нагрева/КЗ нет.

## Этап 4. Прошить Blue Pill И Проверить Static LOW

Этот этап допустим только при снятом J7/HV и разряженной шине.

1. Подключить ST-Link к `J_SWD`: VREF, SWDIO, SWCLK, HOT_GND, NRST.
2. Не подавать питание Blue Pill от ST-Link.
3. Собрать runtime firmware:

```powershell
cd C:\mic_practice\bluepill_uart_pwm_pio
pio run -e bluepill_uart_pwm
```

4. Прошивать только через bus-off preflight wrapper.
5. После прошивки отключить ST-Link перед дальнейшей HV-сборкой.
6. Проверить, что PA8/PA9/PA10/PB13/PB14/PB15 и PB12 удерживаются LOW до валидной команды.

Если используется ROM-загрузчик вместо ST-Link: отключить UART UNO Q, выбрать источник `PC`, установить `BOOT0=1`, перезапустить Blue Pill и выполнить `py -3 -u tools\bluepill_runtime_static_preflight.py --confirm-hv-off --env bluepill_uart_pwm_serial --upload-port COM6`. После прошивки вернуть `BOOT0=0`, перезапустить плату, отключить TX USB-UART от `PA3` и вернуть источник `UNO`.

Критерий перехода: свежая runtime-прошивка, все силовые команды LOW, `EM_STOP` активен.

## Этап 5. Проверить UART Через ISO7721

Выбрать только один источник команд.

### UNO Q

- `D1 TX -> JP_UART_TX.UNO`;
- `D0 RX <- JP_UART_RX.UNO`;
- UNO Q GND остаётся на SAFE стороне;
- джамперы TX/RX установить в положение `UNO`.

### PC-direct

- USB-UART TX/RX/GND подключить к SAFE-разъёму;
- power pin адаптера оставить NC;
- джамперы TX/RX установить в положение `PC`.

Проверить связь на `115200 8N1`, CRC, sequence, timeout и возврат в SAFE при обрыве UART.

Критерий перехода: `link_ok=1`, bad counter не растёт, потеря связи выключает PWM.

## Этап 6. Проверить Аппаратный Запрет PWM

1. Замкнуть нормально-замкнутый SAFE-контур.
2. При `PB12=LOW` проверить `J2-1=LOW`.
3. При `PB12=HIGH` и замкнутом контуре проверить `J2-1=HIGH`.
4. Разомкнуть контур: `J2-1` обязан сразу стать LOW независимо от программы.
5. Снять SAFE_5V: результат также LOW.
6. Снять HOT_3V3: выходная подтяжка должна удержать J2-1 LOW.

Это проверка PWM inhibit, а не силового E-STOP. Отдельно проверить, что настоящая кнопка снимает катушку внешнего сетевого контактора.

Критерий перехода: таблица истинности выполнена аппаратно.

## Этап 7. Подключить STEVAL J2 И Датчики

Подключить J2 строго по таблице `CONNECTION_MATRIX_RU.md`:

- шесть PWM через 33R и 47K pulldown;
- J2-1 через hardware interlock;
- J2-14/15/17/19/26/31/33 через 100R;
- PB5/PB9 через 100R и 47K pulldown; PB1 и J2-21 остаются NC;
- J2-25 = HOT_5V;
- J2-28/29 = HOT_3V3;
- все 13 J2 GND pins = HOT_GND;
- J2-34 оставить NC.

AS5600: `PB10 SCL`, `PB11 SDA`, `HOT_3V3`, `HOT_GND`. В текущей прошивке измерение phase C виртуальное, а PB0 занят температурой J2-26 при SW3=`TSO 1-2`.

Критерий перехода: `enc_ok=1`, температура и ADC не на rails, Vbus при снятом J7 около калиброванного нуля.

## Этап 8. Подключить Изолированный Saleae

Подключить Saleae только к `J_SALEAE`:

- CH0..CH5: шесть PWM после ISO7740;
- CH6: PB12 RUN command;
- CH7: не подключать; PB4/K1 удалены из текущей конфигурации;
- GND: SAFE_GND.

Прямой Saleae на Blue Pill разрешён только как отдельная bus-off диагностическая конфигурация без J7/HV; перед силовой сборкой вернуть изолированное подключение.

Проверить static LOW и затем диагностический PWM с deadtime 800 нс. На каждой фазе одновременно HIGH у верхнего и нижнего канала быть не должно.

Критерий перехода: все три пары без overlap, CH6 LOW в SAFE; CH7 не используется.

## Этап 9. Проверить Отсутствие K1 И Вентилятор Без HV

### Силовой ввод

- K1, драйвер катушки и провод PB4 не устанавливать;
- PB4, PB1 и STEVAL J2-21 оставить NC;
- подтвердить `precharge=0` и `(bp_ext & 0x08)=0`;
- до подачи сети установить отдельный рассчитанный ограничитель пускового тока;
- прямое включение выпрямителя с накопительным конденсатором запрещено.

### Fan

- стандартный 4-pin: GND, +12V, TACH, PWM;
- PB3 через MMBT2222A, 25 кГц;
- PA11 TACH; Blue Pill USB не использовать;
- 3-pin fan работает постоянно на полной скорости.

Критерий перехода: PB4 не активируется, fan duty/RPM читаются, после STOP всё возвращается в безопасное состояние.

## Этап 10. Прозвонить Сетевую Часть

Без сети и без установленного J7:

1. Проверить KBPC5010 по маркировке `+`, `-`, `~`, `~`.
2. Подтвердить `AC_L -> F1 -> BR1.AC1`.
3. Подтвердить `AC_N -> BR1.AC2`.
4. Подтвердить MOV между `AC_L_FUSED` и `AC_N` после F1.
5. Подтвердить отсутствие K1/Q1/Dcoil и соединения PB4.
6. Проверить внешний ограничитель пускового тока по документации его производителя.
7. Подтвердить `BR1.MINUS -> HV_DC-/J7 DC-` без связи с AC_N/PE/SAFE_GND.
8. Проверить четыре последовательных bleeder 100K/0.5W.

Критерий перехода: машинная схема и физическая прозвонка совпадают.

## Этап 11. Обязательный PCB/Layout Review

До 230VAC проверить:

- creepage/clearance для загрязнения и материала платы;
- прорези под изоляторами;
- отсутствие меди под isolation barrier;
- ширину и нагрев HV/моторных дорожек;
- предохранитель F1 по току двигателя и проводам;
- импульсную энергию RPRE1/RPRE2;
- PE, корпус, ввод кабеля, разгрузку натяжения;
- внешний контактор и требуемую категорию E-STOP;
- защиту от прикосновения и закрытые test points.

Пока этот этап не подписан, 230VAC не подавать.

## Этап 12. Условия Для Будущего HV-Пуска

HV-пуск обсуждается только после выполнения этапов 0..11 и отдельного подтверждения оператора. Обязательны внешний контактор/E-STOP, выбранный F1, ограничение тока, исправный предзаряд, закрытый корпус и свежий low-voltage regression. Наличие принципиальной схемы само по себе не разрешает подачу 230VAC.

Для автономного пуска с телефона HMI должен быть установлен командой `py -3 -u tools\adb_deploy_web_hmi.py --restart --standalone-hv`. После перезапуска он всегда не вооружён. Перед пуском проверить реальное напряжение мультиметром и совпадение `bp_vdc`, затем ввести `ARM 310V`. Разрешение действует 30 секунд и выдаётся только при исправных связи, VBUS, температуре и отсутствии аварий. Если HMI не выдаёт разрешение, силовой пуск запрещён; программную блокировку не обходить.
