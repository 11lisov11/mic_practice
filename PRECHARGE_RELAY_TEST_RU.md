# Проверка Реле Предзаряда По MIC_AI.pdf

Это инструкция для физического `RELAY1 SRD-12VDC-SL-C` из схемы `MIC_AI.pdf`.
Не путать с неиспользуемым контактом STEVAL `J2-21 NTC bypass relay`:
`J2-21` и `Blue Pill PB1` должны оставаться `NC`.

## Что Показывает Схема

| Узел | По схеме |
|---|---|
| Управление `RELAY1` | Blue Pill `PB4` |
| Драйвер катушки | `PB4 -> R2 1K -> база Q1 2N2222A`, `R3 47K` база-эмиттер |
| Катушка реле | `DC15V -> R1 50 -> RELAY1 coil -> Q1 collector`, Q1 emitter -> `GND` |
| Защита катушки | `D2 UF4007` параллельно катушке |
| Силовой контакт | шунтирует `R4 + R5` после предзаряда |
| Предзарядные резисторы | `R4 20R 5W` + `R5 20R 5W` последовательно |

## Куда Подключать

### Логическое Управление

| Откуда | Куда |
|---|---|
| Blue Pill `PB4` | вход драйвера реле, узел `PB4` перед `R2 1K` |
| Blue Pill `GND` | общий `GND` драйвера реле |
| Saleae `CH7` | Blue Pill `PB4` или узел `PB4/R2` |
| Saleae `GND` | общий `GND` |

### Катушка Реле

| Узел | Подключение |
|---|---|
| `DC15V` | через `R1 50R` на верхнюю сторону катушки/диода |
| `Q1 collector` | нижняя сторона катушки |
| `Q1 emitter` | `GND` |

Пин-номера `RELAY1` сверять по footprint/плате, а не по тексту корпуса: в схеме катушка показана между узлами `4` и `1`.

### Силовой Контакт

Контакт реле шунтирует цепь предзаряда:

```text
220V after bridge -> R4 20R -> R5 20R -> DC+
                  -> RELAY1 contact bypass -> DC+
```

Для первичного теста реле силовую часть `220V/DC+` не подключать.

## Команда Теста

После прошивки UNO Q и Blue Pill:

```powershell
py -3 -u .\tools\precharge_relay_preflight.py --url http://127.0.0.1:18080 --la-channel 7
```

Критерий:

- `overall_pass=true`;
- `SAFE`;
- `pwm=0`;
- `bp_fault=0`;
- `precharge` переключается `1/0`;
- в `bp_ext` переключается бит `0x08`;
- на Saleae `CH7` видны фронты `PB4`.
