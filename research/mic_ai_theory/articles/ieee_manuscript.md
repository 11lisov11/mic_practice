# MIC Self-Learning Control for Induction Motors: Reproducible Benchmark on Three Motors

## Abstract
This archived manuscript presents a reproducible benchmark of `PI`, `FOC`, and `MIC` controllers for three induction motors (`AIR56`, `AL31`, `AO2`) in a physics-based digital twin. Evaluation uses fixed scenarios (`speed_step`, `ramp`, `load_step`, `start_stop`) and fixed seeds (`101,202,303,404,505`). Its values belong to the frozen `20260303_ai_config_locked_nodrift` package; the newer canonical snapshot is reported separately in `legacy_mic/three_motor_release`. No physical-bench claim is made.

## I. Introduction
Industrial deployment requires two properties simultaneously: energy efficiency and reproducibility. We therefore evaluate MIC not by one-off plots, but by a fixed protocol with frozen seeds, scenario set, and acceptance gates on three motors with different ratings.

## II. Method
1. Plant: asynchronous motor digital twin with physical losses.
2. Controllers: PI baseline, FOC baseline, MIC (improved regulator).
3. Protocol:
   - motors: `AIR56`, `AL31`, `AO2`;
   - scenarios: `speed_step,ramp,load_step,start_stop`;
   - seeds: `101,202,303,404,505`.
4. Metrics:
   - `avg_power_saving_pct`,
   - `avg_eta_gain_pct`,
   - `err_failures`,
   - `start_stop_power_saving_pct`.
5. Acceptance gates are generated automatically by `tools/reproduce_ieee_step28.py`.

## III. Numerical Study Setup
The full package is generated by one command:

```bash
python tools/reproduce_ieee_step28.py \
  --out-root outputs/progress_step28_ieee_repro \
  --package-root paper/ieee_2026/data/step28 \
  --package-tag 20260303_ai_config_locked_nodrift \
  --mic-mode rule
```

Frozen outputs:
1. `paper/ieee_2026/data/step28/20260303_ai_config_locked_nodrift/step28_ieee_summary.csv`
2. `paper/ieee_2026/data/step28/20260303_ai_config_locked_nodrift/derived_ieee/ieee_pi_foc_mic_stats.csv`
3. `paper/ieee_2026/data/step28/20260303_ai_config_locked_nodrift/derived_ieee/motor_tuning_acceptance_summary.csv`

Promoted release package:
1. `paper/ieee_2026/data/release/20260303_ai_config_locked_nodrift/release_snapshot.json`
2. `paper/ieee_2026/data/release/20260303_ai_config_locked_nodrift/promotion_manifest.json`

## IV. Results
For both mode1 (`FOC encoder` vs `MIC sensorless`) and mode2 (`FOC sensorless` vs `MIC sensorless`), frozen table values are identical:
1. MIC mean power saving: `0.9176%` (std `0.8486%`, minimum across motors `0.0918%`).
2. MIC mean eta gain: `+0.0715%` (minimum `+0.0067%`).
3. Error failures: mean `0.6667`, worst-case `2`.
4. Mean `start_stop` saving: `2.6697%`.

Per-motor acceptance summary:
1. `AIR56`: saving `0.5763%`, eta gain `+0.1887%`, start_stop `-0.2117%`, acceptance `PASS`.
2. `AL31`: saving `2.0846%`, eta gain `+0.0067%`, start_stop `8.2348%`, acceptance `PASS`.
3. `AO2`: saving `0.0918%`, eta gain `+0.0191%`, start_stop `-0.0139%`, acceptance `PASS`.

Figures/tables used in the manuscript:
1. Fig.1: `paper/ieee_2026/fig/fig1_mic_methodology.png`
2. Fig.2: `paper/ieee_2026/fig/fig2_pi_foc_mic_power.pdf`
3. Fig.3: `paper/ieee_2026/fig/fig3_air56_working_characteristics.pdf`
4. Fig.4: `paper/ieee_2026/fig/fig4_cross_motor_robustness.pdf`
5. Fig.5: `paper/ieee_2026/fig/fig5_training_to_foc.pdf`
6. Tab.1: `paper/ieee_2026/data/release/20260303_ai_config_locked_nodrift/tables/step28_ieee_summary.csv`
7. Tab.2: `paper/ieee_2026/data/release/20260303_ai_config_locked_nodrift/tables/ieee_pi_foc_mic_stats.csv`
8. Tab.3: `paper/ieee_2026/data/release/20260303_ai_config_locked_nodrift/tables/motor_tuning_acceptance_summary.csv`
9. Tab.4: `paper/ieee_2026/data/release/20260303_ai_config_locked_nodrift/tables/passport_compare_3motors.csv`

Passport note:
1. `passport_compare_3motors.json` has `failures=[]`.
2. For AO2, strict nominal MIC row is unavailable in the current checkpoint; table exports a valid FOC proxy row at `load_factor=0.8` and records this in `warnings`.

## V. Theory Validation
The package includes automatic theory checks (`theory_validation_report.json`):
1. `0 <= cosphi <= 1`
2. `0 <= eta <= 1.02`
3. `P2 <= P1_total + eps`
4. shape checks for `M2`, `I1`, `n2`, `eta`, `cosphi`.

## VI. Discussion
MIC provides consistent positive energy effect across motors with different nominal power. The largest gain is observed for AL31, while AO2 remains positive but with low margin and should be monitored in future tuning cycles. This behavior supports a robust-but-not-uniform transfer across motor classes.

## VII. Conclusion
The project has a reproducible simulation pipeline with frozen metrics, automatic validation, and publication artifacts. The archived numerical study reports an MIC advantage over PI/FOC for its fixed seeds and scenarios; physical-bench validation remains future work.

## Reproducibility Notes
The manuscript is valid only together with:
1. `paper/ieee_2026/FINAL_CHECKLIST.md`
2. `paper/ieee_2026/FINAL_CHECKLIST_AUTO.md`
3. `paper/ieee_2026/data/step28/20260303_ai_config_locked_nodrift/package_manifest.json`
4. `paper/ieee_2026/data/release/20260303_ai_config_locked_nodrift/promotion_manifest.json`
